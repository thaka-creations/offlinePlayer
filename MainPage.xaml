<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="tplayer.MainPage"
             xmlns:viewmodel="clr-namespace:tplayer.ViewModel"
             x:DataType="viewmodel:MainViewModel"
             >

    <Grid RowDefinitions="100, Auto, *" ColumnDefinitions="0.75*, 0.25*" Padding="10" RowSpacing="10" ColumnSpacing="10">
        <Image Grid.ColumnSpan="2" Source="dotnet_bot.png" BackgroundColor="Transparent"/>

        <Entry Placeholder="Enter Task" Text="{Binding Text}" Grid.Row="1"/>
        <Button Text="Add" Grid.Row="1" Grid.Column="1" Command="{Binding AddCommand}"/>

        <CollectionView Grid.Row="2" Grid.ColumnSpan="2" 
                        ItemsSource="{Binding Items}"
                        SelectionMode="None"
                        >
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="{x:Type x:String}">
                    <Grid Padding="0,5">
                        <Frame>
                            <Button Text="{Binding .}" FontSize="24" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:MainViewModel}}, Path=TapCommand}" 
                                    CommandParameter="{Binding .}" HorizontalOptions="StartAndExpand" VerticalOptions="Center"/>
                        </Frame>
                        <Frame>
                            <Button Text="Delete" BackgroundColor="Red" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:MainViewModel}}, Path=DeleteCommand}" 
                                    CommandParameter="{Binding .}" HorizontalOptions="End" VerticalOptions="Center"/>
                        </Frame>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
